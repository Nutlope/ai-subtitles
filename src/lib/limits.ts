/** Maximum upload file size in bytes */
export const MAX_FILE_SIZE = 500 * 1024 * 1024; // 500MB

/** Human-readable file size limit for UI */
export const MAX_FILE_SIZE_LABEL = '500MB';

/** Maximum media duration (seconds) for free run without a user API key */
export const MAX_FREE_RUN_DURATION_SECONDS = 60 * 60; // 1 hour

/** Human-readable free duration limit for UI/errors */
export const MAX_FREE_RUN_DURATION_LABEL = '1 hour';
